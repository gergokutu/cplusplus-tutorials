#include <iostream>
#include <string>

int main()
{
  // loops >> things are repeated
  // ICU
  // Initialize a variable
  // Condition
  // Update

  // while
  // 0 execution can happen
  // condition than execution
  // want to iterate indefinetly
  // e.g till sg is true...
  int i = 0;
  while (i <3 )
  {
    std::cout << "With wile loop\n";
    i++;
  }

  // do-while >> at least 1 execution
  // execution then condition

  // for
  // usually when you know 
  // the number of iterations ahead of time

  for (i = 0; i < 3; i++)
  {
    std::cout << "With for loop\n";
  }

  // indefinate loop
  // while (true)... break out...
}