// basics for the improvement of the guessing game
#include <iostream>

int main()
{
  // define an array of integers
  // which can hold 100 integers
  // size 100 is index 99 (size -1) >> array is 0 indexed
  // statically sized >> when it is compiled the size (storage) is set
  // arrays has no knowledge about their size
  int guesses[100];

  // if you want a dinamic array >> use vectors
  // vectors is other programming languages: lists, list arrays
  // you can add more elements later (more than the size)
  // use this if you do not know the size ahead of time
  // knowledge about their size

  // templetized array
  // normal array wrapped inside of an object
  // you can easily keep track of the size of the array
}