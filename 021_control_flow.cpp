// in the following videos we are going through
// the key control flow structures
// 2 main:
// branching >> if (else if, else), switch (case, default, break)
// looping >> while, for, do-while
#include <iostream>

int main()
{
  if (expression) // true/false...
  {
    // code
  } 
  else if (expression_2)
  {
    // code
  } 
  else
  {
    // code
  }

  // age >> has to be an integral type
  // javascript >> it also can be a string...
  switch(age) 
  {
    case 17:
      // code
      break;
    case 18:
      // code
      break;
    default:
      // code
      break;
  }
}